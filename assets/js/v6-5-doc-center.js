(function(){
  const $=(s,root=document)=>root.querySelector(s);
  const $$=(s,root=document)=>[...root.querySelectorAll(s)];
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const money=n=>Number(n||0).toLocaleString(undefined,{style:'currency',currency:'USD'});
  function project(){return {title:$('[data-ws-title]')?.textContent?.trim()||'Selected Project',meta:$('[data-ws-meta]')?.textContent?.trim()||'',status:$('[data-ws-status]')?.textContent?.trim()||'Planning',notes:$('[data-ws-notes]')?.textContent?.trim()||''};}
  function estimateRows(){
    const rows=$$('[data-estimate-list] .line-table > div').map(r=>$$('span,b',r).map(x=>x.textContent.trim()));
    return rows.map(c=>({description:c[0]||'',qty:c[1]||'1',unit:c[2]||'$0.00',total:c[3]||'$0.00'}));
  }
  function invoices(){return $$('[data-invoice-list] .mini-row').map(r=>({title:$('b',r)?.textContent||'Invoice',meta:$('span',r)?.textContent||''}));}
  function schedule(){return $$('[data-schedule-list] .mini-row').map(r=>({title:$('b',r)?.textContent||'Event',meta:$('span',r)?.textContent||''}));}
  function docCSS(){return `<style>body{font-family:Arial,sans-serif;color:#101827;margin:0;background:#f4f7fb}.page{background:#fff;max-width:850px;margin:24px auto;padding:42px;border:1px solid #d9e2ef}.top{display:flex;justify-content:space-between;gap:24px;border-bottom:4px solid #f5c400;padding-bottom:18px}.brand{font-size:24px;font-weight:900;color:#062a52}.small{color:#5c6675;font-size:12px;line-height:1.5}.badge{display:inline-block;background:#062a52;color:#fff;padding:6px 10px;border-radius:999px;font-size:12px}h1{color:#062a52;margin:28px 0 8px;font-size:32px}h2{color:#062a52;border-bottom:1px solid #e6edf5;padding-bottom:8px;margin-top:28px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.box{border:1px solid #e2e8f0;border-radius:12px;padding:14px;background:#f8fafc}.line{display:grid;grid-template-columns:1fr 80px 110px 110px;gap:12px;border-bottom:1px solid #e5e7eb;padding:10px 0}.line.head{font-weight:800;color:#062a52;background:#f8fafc;padding:10px}.total{text-align:right;font-size:22px;font-weight:900;color:#062a52;margin-top:18px}.sig{margin-top:50px;display:grid;grid-template-columns:1fr 1fr;gap:40px}.sig div{border-top:1px solid #111;padding-top:8px}.water{position:fixed;right:22px;bottom:22px;color:#b9c4d6;font-weight:900}.btns{max-width:850px;margin:18px auto;display:flex;gap:10px}.btn{border:0;border-radius:10px;background:#062a52;color:white;padding:10px 14px;font-weight:800}.yellow{background:#f5c400;color:#061323}@media print{.btns{display:none}.page{margin:0;border:0;max-width:none}.water{display:none}}</style>`;}
  function baseHeader(type){const p=project(); return `<div class="top"><div><div class="brand">BLUE BEAR ELECTRIC</div><div class="small">CA License #1141313 • OSHA Certified • Bonded & Insured • Union Contractor<br>760-234-8306 • Imperial County, CA</div></div><div style="text-align:right"><span class="badge">${esc(type)}</span><div class="small">Generated ${new Date().toLocaleDateString()}</div></div></div><h1>${esc(type)}</h1><div class="grid"><div class="box"><b>Project</b><br>${esc(p.title)}<br><span class="small">${esc(p.meta)}</span></div><div class="box"><b>Status</b><br>${esc(p.status)}<br><span class="small">VoltFlow document export</span></div></div>`;}
  function lineTable(){const rows=estimateRows(); const total=rows.reduce((s,r)=>s+Number(String(r.total).replace(/[^0-9.-]/g,'')),0); return `<div class="line head"><span>Description</span><span>Qty</span><span>Unit</span><span>Total</span></div>${rows.length?rows.map(r=>`<div class="line"><span>${esc(r.description)}</span><span>${esc(r.qty)}</span><span>${esc(r.unit)}</span><b>${esc(r.total)}</b></div>`).join(''):'<p class="small">No estimate items added yet.</p>'}<div class="total">Total: ${money(total)}</div>`;}
  function proposalBody(){const p=project(); return `${baseHeader('Customer Proposal')}<h2>Project Summary</h2><p>Blue Bear Electric will provide professional electrical services for the selected project. Work may include planning, troubleshooting, installation, repair, maintenance, testing, documentation, and closeout support depending on approved scope and field conditions.</p><h2>Scope Notes</h2><p>${esc(p.notes||'Scope notes to be finalized after field review.')}</p><h2>Estimate Items</h2>${lineTable()}<h2>Safety and Quality</h2><p>Work will be completed with safe electrical practices, jobsite coordination, appropriate PPE, clean workmanship, and project documentation. Licensing and certificates are available upon request.</p><h2>Next Steps</h2><ol><li>Confirm final scope and site access.</li><li>Approve estimate.</li><li>Schedule work.</li><li>Complete installation/service.</li><li>Provide closeout documentation and invoice.</li></ol><div class="sig"><div>Customer Approval</div><div>Blue Bear Electric</div></div>`;}
  function estimateBody(){return `${baseHeader('Estimate')}<h2>Estimate Line Items</h2>${lineTable()}<h2>Terms</h2><p class="small">Pricing is subject to field conditions, material availability, utility coordination, permit requirements, and customer-approved scope. This document is a working estimate unless otherwise signed and accepted.</p><div class="sig"><div>Customer Approval</div><div>Date</div></div>`;}
  function invoiceBody(){const inv=invoices(); return `${baseHeader('Invoice')}<h2>Invoice Summary</h2>${lineTable()}<h2>Invoice Records</h2>${inv.length?inv.map(i=>`<div class="box"><b>${esc(i.title)}</b><br><span class="small">${esc(i.meta)}</span></div>`).join('<br>'):'<p class="small">No invoice records found for this project.</p>'}<div class="sig"><div>Received By</div><div>Date</div></div>`;}
  function closeoutBody(){const p=project(); const sch=schedule(); return `${baseHeader('Project Closeout Package')}<h2>Project Overview</h2><p>${esc(p.notes||'Project notes pending.')}</p><h2>Checklist</h2><ul><li>Before photos saved</li><li>After photos saved</li><li>Estimate and invoice reviewed</li><li>Customer approval documented</li><li>Warranty notes added</li><li>Final inspection complete</li></ul><h2>Schedule History</h2>${sch.length?sch.map(e=>`<div class="box"><b>${esc(e.title)}</b><br><span class="small">${esc(e.meta)}</span></div>`).join('<br>'):'<p class="small">No schedule events found.</p>'}<h2>Closeout Notes</h2><p>Final closeout package should include relevant project photos, testing notes, warranty notes, inspection status, and customer communication.</p><div class="sig"><div>Customer Acceptance</div><div>Blue Bear Electric</div></div>`;}
  function emailBody(){const p=project(); return `Subject: Blue Bear Electric - ${p.title}\n\nHello,\n\nThank you for working with Blue Bear Electric. Attached is the document package for ${p.title}.\n\nProject status: ${p.status}\nProject info: ${p.meta}\n\nPlease review and let us know if you have any questions or requested changes.\n\nThank you,\nBlue Bear Electric\n760-234-8306`;}
  function openDoc(type){let body=proposalBody(); if(type==='estimate')body=estimateBody(); if(type==='invoice')body=invoiceBody(); if(type==='closeout')body=closeoutBody(); const w=window.open('','_blank'); w.document.write(`<!doctype html><html><head><title>VoltFlow ${type}</title>${docCSS()}</head><body><div class="btns"><button class="btn yellow" onclick="window.print()">Print / Save PDF</button><button class="btn" onclick="document.body.classList.toggle('compact')">Toggle View</button></div><main class="page">${body}</main><div class="water">VOLTFLOW</div></body></html>`); w.document.close();}
  function renderPreview(type='proposal'){const box=$('[data-doc-preview]'); if(!box)return; let text=''; if(type==='proposal')text=proposalBody(); if(type==='estimate')text=estimateBody(); if(type==='invoice')text=invoiceBody(); if(type==='closeout')text=closeoutBody(); if(type==='email')text='<pre>'+esc(emailBody())+'</pre>'; box.innerHTML=text;}
  function downloadHTML(type){let body=proposalBody(); if(type==='estimate')body=estimateBody(); if(type==='invoice')body=invoiceBody(); if(type==='closeout')body=closeoutBody(); const html=`<!doctype html><html><head><meta charset="utf-8"><title>VoltFlow ${type}</title>${docCSS()}</head><body><main class="page">${body}</main></body></html>`; const blob=new Blob([html],{type:'text/html'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=`voltflow-${type}-${Date.now()}.html`; a.click(); URL.revokeObjectURL(a.href);}
  document.addEventListener('click',e=>{
    const btn=e.target.closest('[data-doc-type]'); if(btn){$$('[data-doc-type]').forEach(b=>b.classList.toggle('active',b===btn)); renderPreview(btn.dataset.docType);}
    const open=e.target.closest('[data-open-doc]'); if(open)openDoc(open.dataset.openDoc);
    const dl=e.target.closest('[data-download-doc]'); if(dl)downloadHTML(dl.dataset.downloadDoc);
    if(e.target.matches('[data-copy-customer-email]')) navigator.clipboard?.writeText(emailBody());
  });
  document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>renderPreview('proposal'),900));
})();
